/**
 * Dedicate to generate terrain, which merges all unit and remove unneccesary faces like down-side
 * and adjacent ones.
 * TODO:
 *   o Consider flat terrain only first
 *   o two pass generation: one for position generation, one for face merge and cut.
 *   o make terrain data load&store-able.
 *   o consider complex terrain generation.
 */
